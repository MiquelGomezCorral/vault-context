{
  "name": "opencode-vault-context",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight Obsidian RAG context injector for opencode",
  "scripts": {
    "check": "node --check vault-context.js",
    "benchmark": "node scripts/benchmark.mjs",
    "benchmark:orig": "node scripts/benchmark.mjs --kw=168"
  },
  "engines": {
    "node": ">=20"
  }
}
